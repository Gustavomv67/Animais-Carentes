@model IEnumerable<Animais_Carentes.Models.AgendamentoModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dataEntrega)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuarioEntrega)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.enderecoEntrega)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.animal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dataEntrega)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuarioEntrega)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.enderecoEntrega)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.animal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
